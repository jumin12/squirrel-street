# Render Blueprint — Link a PostgreSQL instance in the dashboard and set DATABASE_URL on this service.
services:
  - type: web
    name: squirrel-street-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
