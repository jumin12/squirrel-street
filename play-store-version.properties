# Google Play: set these in android/app/build.gradle inside defaultConfig { }:
#   versionCode <VERSION_CODE>   (must increase every Play upload)
#   versionName "<VERSION_NAME>" (keep aligned with package.json "version")
# In-game menu/changelog use the same marketing version as VERSION_NAME where possible.
VERSION_CODE=22
VERSION_NAME=2.2.0
